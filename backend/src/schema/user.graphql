## Doesn't work ##

type Query {
  user(id: ID!): User
  users: [User]!
  me: User
}

type Mutation {
  signUp(email: String!, password: String!): User!
  signIn(email: String!, password: String!): User!
  signout: Boolean!
  me: User
}

type User {
  id: ID!
  email: String!
}
