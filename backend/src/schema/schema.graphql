# import * from './schema_prep.graphql'

type Mutation {
  signup(email: String!, password: String!, name: String!): User!
}

type Query {
  users: [User]!
}

type User {
  id: ID!
  email: String!
}
