import { Meta } from '@storybook/react';

import Expand from './Expand';

const story = { component: Expand, title: 'DESIGN/ICONS/Expand' };

export const expand = () => <Expand />;

export default story as Meta;
