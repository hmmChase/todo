import SignUpModal from './SignUpModal';

const story = { component: SignUpModal, title: 'USER/SignUpModal' };

export const signUpModal = () => <SignUpModal close={() => {}} />;

export default story;
