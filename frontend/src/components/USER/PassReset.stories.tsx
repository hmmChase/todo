import PassReset from './PassReset';

const story = { component: PassReset, title: 'USER/PassReset' };

export const passReset = () => <PassReset passResetToken='passResetToken' />;

export default story;
