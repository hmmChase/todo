import IdeaDetailIcon from './IdeaDetailIcon';

const story = {
  component: IdeaDetailIcon,
  title: 'IDEA/DETAIL/IdeaDetailIcon'
};

export const ideaDetailIcon = () => <IdeaDetailIcon ideaId={'ideaId'} />;

export default story;
